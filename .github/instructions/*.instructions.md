Files to ignore:
* Ignore the Install-All-Scripts.sql and Install-Azure.sql files. These are generated automatically by the release process.
* Ignore the Deprecated folder. These scripts only exist for old versions of our product and are no longer maintained.
* Ignore lines in the scripts that set the @Version and @VersionDate parameters. Those are maintained automatically by the release process.
